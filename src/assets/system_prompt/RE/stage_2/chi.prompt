根据给定的句子，如果存在关系，则识别出两个类型为 ("$stl", "$otl") 的实体，以及它们之间的关系为 "$relation"。

根据原句子，从其中找出存在的类型和关系组，然后列出来！

如果存在多个关系组，请按组列出所有关系组。如果不存在，则回答“无”。

输出必须采用表格格式,表头为stl, otl, reaction。

每行必须恰好包含一个主语-宾语对。如果存在多个关系，请将它们分行显示。

请勿添加解释或摘要。仅严格按要求输出。